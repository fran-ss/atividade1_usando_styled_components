import { useState } from "react";

type Post = {
  id: string;
  description: string;
  done: boolean;
};

export default function Home(){
  const [posts, setposts] = useState([])
  const []
  
}
function handleAdd() {
  if (post) {
    const 
  }
}